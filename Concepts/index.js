/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import Blur from './components/Blur';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Blur);
